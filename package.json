{
  "name": "drone-show-simulator",
  "version": "1.0.0",
  "description": "Generative Drone Show Simulator - Create animated drone light shows from text",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:surge": "npm run build && npx surge dist drone-show.surge.sh",
    "deploy:vercel": "npm run build && npx vercel dist",
    "deploy:netlify": "npm run build && npx netlify deploy --prod --dir=dist"
  },
  "dependencies": {
    "three": "^0.169.0",
    "gsap": "^3.12.5",
    "dat.gui": "^0.7.9"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "vite": "^5.4.0"
  }
}